services:
  lightrag:
    volumes:
      - ./lightrag/operate.py:/app/lightrag/operate.py
      - ./lightrag/api/config.py:/app/lightrag/api/config.py
      - ./lightrag/api/lightrag_server.py:/app/lightrag/api/lightrag_server.py

  vllm-embed:
    image: vllm/vllm-openai:latest
    runtime: nvidia
    command: >
      Qwen/Qwen3-Embedding-0.6B
      --runner pooling
      --convert embed
      --port 8001
      --dtype float16
      --gpu-memory-utilization 0.85
    environment:
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
    ports:
      - "8001:8001"
    volumes:
      - ./data/hf-cache:/root/.cache/huggingface
    ipc: host
    healthcheck:
      test:
        - CMD-SHELL
        - 'PORT_HEX="$(printf ''%04X'' 8001)"; cat /proc/net/tcp /proc/net/tcp6 2>/dev/null | grep -q ":$${PORT_HEX} "'
      interval: 5s
      timeout: 3s
      retries: 120
      start_period: 10s
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
